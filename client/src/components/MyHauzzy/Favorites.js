import React from 'react';

class Favorites extends React.Component {
  render() {
    return <h2>Saved properties</h2>
  }
}

// Favorites.propTypes = {
//   history: PropTypes.object.isRequired
// }

export default Favorites;